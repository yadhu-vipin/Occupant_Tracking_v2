# Embeddings tier -- adds the face model, needed only to regenerate data:
#   pipeline/01_build_dataset_splits.py     (detector-in-the-loop photo selection)
#   pipeline/02_extract_embeddings_arcface.py
#
# steps 03-07 do NOT need this -- the shipped embeddings_arcface/ + dataset_splits/
# are already built and consistent.
-r requirements.txt
opencv-python==5.0.0.93
insightface==1.0.1
onnxruntime==1.29.0      # swap for onnxruntime-gpu if the CUDA runtime is available
tqdm==4.70.0
